**Argument guard.** The ticket argument is `{{arg}}`. Before anything else, confirm it is a single well-formed ticket id — {{#if project.ticketPrefix}}`{{upper project.ticketPrefix}}-<number>`{{else}}`<PREFIX>-<number>` (e.g. `{{eg.prefixUpper}}-312`){{/if}}. If it is empty, multi-word, or otherwise malformed (argument substitution can mangle multi-word invocations), do not use it verbatim: recover the intended id from the invocation text or the recent conversation and say which id you recovered; if none can be recovered, ask the user. A malformed id must never reach backend calls, branch names, or commit messages.
