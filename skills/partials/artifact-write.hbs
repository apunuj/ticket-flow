**Update the work artifact.** {{op "upsertWorkArtifact"}}. Set `status`, tick the US-N / T-N checkboxes that are now done, refresh `updated`, and fill the `PR` / `Review verdict` sections as those become available.{{#if output.inlineArtifacts}} Then render the updated artifact sections (user stories / acceptance criteria / plan, with current checkbox state) inline in your reply — the user must never need to open the ticket to see current state.{{/if}}
