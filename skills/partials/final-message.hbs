{{#if output.inlineArtifacts}}**End on the render, not a receipt.** This phase is not done until the artifact render above is the turn's final message — never end the turn on a bare receipt (e.g. "artifact updated", a comment link) that just points back at earlier, possibly mid-turn and possibly-hidden text.{{/if}}
